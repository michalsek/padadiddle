import { Screen, Text } from '@/ui';

/**
 * Main app screen.
 * Renders the exact welcome text requested for the initial route.
 */
export default function IndexScreen() {
  return (
    <Screen>
      <Text>Welcome to padadiddle</Text>
    </Screen>
  );
}
