import { Text } from 'react-native';

/**
 * Editor detail screen.
 * Route param `id` points to the specific editable entity.
 */
export default function EditorScreen() {
  return <Text>Editor</Text>;
}
